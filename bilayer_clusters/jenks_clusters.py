import numpy as np
#cimport numpy as np

def jenks_trial(arr, k): #k refers to number of clusters
    arr.sort()
    
    output = []
    SDAM = np.sum((arr - np.mean(arr))**2)

def jenks_clusters(lipids, k, t):

    return
